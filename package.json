{
  "scripts": {
    "f": "oxfmt --write .",
    "f:check": "oxfmt --check .",
    "prepare": "test -d .git && husky || true",
    "bs": "chmod +x scripts/bootstrap.sh && scripts/bootstrap.sh",
    "upd": "apt update && apt upgrade -y && bun upgrade && bun update --latest && bun i && bun f"
  },
  "dependencies": {},
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.4.1",
    "oxfmt": "^0.66.0"
  }
}
