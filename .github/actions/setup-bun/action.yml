name: "Setup Bun"
description: "Setup Bun and install dependencies"
runs:
  using: "composite"
  steps:
    - name: Setup Bun
      uses: oven-sh/setup-bun@v2.0.0
      with:
        bun-version: latest

    - name: Install dependencies
      run: bun i
      shell: bash
