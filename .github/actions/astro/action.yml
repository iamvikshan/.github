name: "Deploy Astro Site to GitHub Pages"
description: "Build and deploy an Astro static site to GitHub Pages using Bun"
inputs:
  node-version:
    description: "Node.js version to use"
    required: false
    default: "20"
  package-manager:
    description: "Package manager to use (bun, npm, pnpm, yarn)"
    required: false
    default: "bun@latest"
  path:
    description: "Root location of your Astro project"
    required: false
    default: "."
runs:
  using: "composite"
  steps:
    - name: Checkout repository
      uses: actions/checkout@v6.0.1

    - name: Install, build, and upload site
      uses: withastro/action@v5.0.1
      with:
        path: ${{ inputs.path }}
        node-version: ${{ inputs.node-version }}
        package-manager: ${{ inputs.package-manager }}

    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4.0.5
